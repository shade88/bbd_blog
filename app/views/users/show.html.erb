<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<p>
  <b>Password:</b>
  <%= @user.password %>
</p>

<p>
  <b>Is admin:</b>
  <%= @user.is_admin %>
</p>

<% @user.blog_messages.each do |post|%>
    <p>
      <b>Post <%= post.id %></b>
      <%= post.text %>
    </p>
   <% end %>

<%= link_to 'Create post', :controller => :blog_messages, :action => :new, :id=> @user.id %> |
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
